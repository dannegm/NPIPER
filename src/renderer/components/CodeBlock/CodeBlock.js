import React from 'react';
import PropTypes from 'prop-types';

import {} from './CodeBlock.styled';

const CodeBlock = ({ ...props }) => {
    return <>{/* TODO: Content */}</>;
};

CodeBlock.propTypes = {
    // TODO: Props Definition
};

CodeBlock.defaultProps = {
    // TODO: Default Props Values
};

export default CodeBlock;
