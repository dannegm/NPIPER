import React from 'react';
import PropTypes from 'prop-types';

import {} from './TitleBar.styled';

const TitleBar = ({ ...props }) => {
    return <>{/* TODO: Content */}</>;
};

TitleBar.propTypes = {
    // TODO: Props Definition
};

TitleBar.defaultProps = {
    // TODO: Default Props Values
};

export default TitleBar;
